import {getAll,put,del} from './storage.js';
import {uid,esc,toast} from './utils.js';
const folders=[['teacher','To Do Leerkracht'],['today','To Do vandaag'],['students','To Do voor de leerlingen']];
export async function renderTodos(root){let current='today';root.innerHTML=`<div class="topbar"><div><div class="title">To Do</div><div class="subtitle">Drie vaste lijsten voor school.</div></div></div><div class="todo-tabs">${folders.map(([id,n])=>`<button data-folder="${id}" class="${id===current?'active':''}">${n}</button>`).join('')}</div><div class="card"><div class="row"><input id="todoInput" class="input grow" placeholder="Nieuwe taak..."><button id="addTodo" class="btn">Toevoegen</button></div></div><div id="todoList" class="list"></div><div class="row" style="margin-top:12px"><button id="clearDone" class="btn secondary small">Wis afgeronde taken</button></div>`;
const input=root.querySelector('#todoInput'),list=root.querySelector('#todoList');
async function draw(){root.querySelectorAll('[data-folder]').forEach(b=>b.classList.toggle('active',b.dataset.folder===current));const items=(await getAll('todos')).filter(x=>x.folder===current).sort((a,b)=>a.done-b.done||a.createdAt.localeCompare(b.createdAt));list.innerHTML=items.length?items.map(x=>`<div class="item todo-item ${x.done?'done':''}"><label class="todo-check"><input type="checkbox" data-check="${x.id}" ${x.done?'checked':''}><span>${esc(x.text)}</span></label><button class="icon-btn danger-text" data-del="${x.id}" aria-label="Verwijderen">×</button></div>`).join(''):'<div class="muted">Nog geen taken in deze lijst.</div>';list.querySelectorAll('[data-check]').forEach(c=>c.onchange=async()=>{const x=items.find(i=>i.id===c.dataset.check);x.done=c.checked;x.updatedAt=new Date().toISOString();await put('todos',x);draw()});list.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{await del('todos',b.dataset.del);draw()})}
async function add(){const text=input.value.trim();if(!text)return;await put('todos',{id:uid(),folder:current,text,done:false,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});input.value='';draw()}
root.querySelector('#addTodo').onclick=add;input.onkeydown=e=>{if(e.key==='Enter')add()};root.querySelectorAll('[data-folder]').forEach(b=>b.onclick=()=>{current=b.dataset.folder;draw()});root.querySelector('#clearDone').onclick=async()=>{const all=await getAll('todos');for(const x of all.filter(i=>i.folder===current&&i.done))await del('todos',x.id);toast('Afgeronde taken gewist');draw()};draw()}
