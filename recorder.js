export class Recorder{constructor(onTick){this.onTick=onTick}async start(){if(!navigator.mediaDevices?.getUserMedia)throw new Error('Microfoonopname wordt niet ondersteund.');this.stream=await navigator.mediaDevices.getUserMedia({audio:true});const types=['audio/mp4','audio/webm;codecs=opus','audio/webm'];const type=types.find(t=>MediaRecorder.isTypeSupported?.(t))||'';this.chunks=[];this.rec=new MediaRecorder(this.stream,type?{mimeType:type,audioBitsPerSecond:48000}:{audioBitsPerSecond:48000});this.rec.ondataavailable=e=>e.data.size&&this.chunks.push(e.data);this.started=Date.now();this.rec.start();this.timer=setInterval(()=>this.onTick?.(Math.floor((Date.now()-this.started)/1000)),500)}stop(){return new Promise(resolve=>{if(!this.rec)return resolve(null);this.rec.onstop=()=>{clearInterval(this.timer);this.stream?.getTracks().forEach(t=>t.stop());resolve(new Blob(this.chunks,{type:this.rec.mimeType||'audio/webm'}))};this.rec.stop()})}}
