import {getAll,put,del,settings} from './storage.js';
import {uid,fmt,esc,mailto,copy,toast} from './utils.js';
export async function renderNotes(root){let editId=null;root.innerHTML=`<div class="topbar"><div><div class="title">Notities</div><div class="subtitle">Snel vastleggen, lokaal opgeslagen.</div></div></div><div class="card"><div class="section-title">Nieuwe notitie</div><textarea id="noteText" class="textarea" placeholder="Schrijf je notitie..."></textarea><div class="row"><button class="btn" id="saveNote">Opslaan</button><button class="btn secondary" id="clearNote">Wissen</button></div></div><div class="field"><input id="searchNotes" class="input" placeholder="Zoek in notities"></div><div class="section-title">Opgeslagen notities</div><div id="notesList" class="list"></div>`;
const text=root.querySelector('#noteText'),list=root.querySelector('#notesList');
async function draw(){const q=root.querySelector('#searchNotes').value.toLowerCase();const notes=(await getAll('notes')).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)).filter(n=>n.text.toLowerCase().includes(q));list.innerHTML=notes.length?notes.map(n=>`<div class="item"><div class="item-head"><div><strong>${esc(n.text.slice(0,70))}${n.text.length>70?'…':''}</strong><div class="muted">${fmt(n.updatedAt)}</div></div></div><div class="row" style="margin-top:10px"><button class="btn secondary small" data-edit="${n.id}">Bewerken</button><button class="btn secondary small" data-copy="${n.id}">Kopiëren</button><button class="btn secondary small" data-mail="${n.id}">Mailen</button><button class="btn danger small" data-del="${n.id}">Verwijderen</button></div></div>`).join(''):'<div class="muted">Nog geen notities.</div>';for(const b of list.querySelectorAll('[data-edit]'))b.onclick=()=>{const n=notes.find(x=>x.id===b.dataset.edit);editId=n.id;text.value=n.text;text.focus()};for(const b of list.querySelectorAll('[data-copy]'))b.onclick=()=>copy(notes.find(x=>x.id===b.dataset.copy).text);for(const b of list.querySelectorAll('[data-mail]'))b.onclick=()=>{const n=notes.find(x=>x.id===b.dataset.mail);mailto(settings.get('email',''),`Notitie – ${new Date(n.updatedAt).toLocaleDateString('nl-NL')}`,n.text)};for(const b of list.querySelectorAll('[data-del]'))b.onclick=async()=>{if(confirm('Deze notitie verwijderen?')){await del('notes',b.dataset.del);draw()}}}
root.querySelector('#saveNote').onclick=async()=>{if(!text.value.trim())return toast('Schrijf eerst iets.');const now=new Date().toISOString();await put('notes',{id:editId||uid(),text:text.value.trim(),createdAt:now,updatedAt:now});editId=null;text.value='';toast('Notitie opgeslagen');draw()};root.querySelector('#clearNote').onclick=()=>{editId=null;text.value=''};root.querySelector('#searchNotes').oninput=draw;draw()}
